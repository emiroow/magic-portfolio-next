import Tab from "@/components/ui/tab";

const page = () => {
  return (
    <main className="w-full flex justify-center flex-col ">
      <p className="text-center text-5xl font-bold">Dashboard</p>
      <Tab />
    </main>
  );
};

export default page;
